# CLAUDE.md

Claude Code project memory file.

Follow @AGENTS.md for the shared repository instructions.

The app shell is intentionally thin. UI modules live in `src/ui/` and workflow orchestration lives in `src/services/`.
